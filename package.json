{
  "name": "loomify-webapp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --port 3000 --turbo",
    "dev:hot": "next dev --port 3000 --turbo",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "generate-manifest": "node scripts/generate-image-manifest.js",
    "export": "npm run generate-manifest && next build && next export",
    "deploy:gh": "npm run export && npx gh-pages -d out -b gh-pages"
  },
  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "next": "15.5.4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.5.4",
    "@eslint/eslintrc": "^3"
    ,"gh-pages": "^5"
  }
}
